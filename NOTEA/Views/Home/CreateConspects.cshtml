@model NOTEA.Models.ConspectModel


@{
    ViewData["Title"] = "Create conspects";
}

<h1>Create conspects</h1>
<hr />
<div class="row">
    <div class="col-md-4">
       @*  <form asp-action="CreateConspects">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Name" class="control-label"></label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form> *@
        <form asp-controller="Home" asp-action="CreateConspects">
            <div class="form-group">
                <label asp-for="Date" class="control-label"></label>
                <input asp-for="Date" class="form-control" />
                <label asp-for="Name" class="control-label"></label>
                <input asp-for="Name" class="form-control" />
                <label asp-for="ConspectText" class="control-label"></label>
                <textarea  asp-for="ConspectText" cols="150" rows="20"></textarea>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
        @* @Html.TextAreaFor(m => m.Date)
        @Html.TextAreaFor(m => m.Name)
        @Html.TextAreaFor(m => m.ConspectText)
        <div class="form-group" asp-action="Save">
            <input type="submit" value="Save" class="btn btn-primary" />
        </div> *@
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}

    <script type="text/javascript">
        function openSuccessModal(strMessage) {
            var myDiv = document.getElementById("MyModalSuccessAlertBody");
            myDiv.innerHTML = strMessage;
            $('#myModalSuccess').modal('show');
        }
        $(document).ready(function () {
            var msg = "@TempData["SuccessMessage"]";
            if (msg)
                openSuccessModal(msg);
        });
    </script>
}
